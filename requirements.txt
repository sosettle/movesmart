# Core app + data pipeline
streamlit
pandas
numpy
requests
urllib3
folium
streamlit-folium
plotly
openpyxl

# PLACES loader (spatial join)
geopandas

# Clustering + scores (final_dataset_loader → models.cluster_model)
scikit-learn

# Optional: wiki_text_loader (AWS Bedrock)
boto3
